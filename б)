#include <iostream>
#include <cmath>

using namespace std;


int main() {
    float x, y,z;
    cin>>x>>y;
    z = sqrt(x*x+y*y);
    if ((x >=-2 && x<=2) && (y >=-2 && y<=2) && !(z >=0 && z<=2)){
        cout<<"Попал";
    }
    else{
        cout<<"Не попал";
    }


    return 0;
}
