#include <iostream>
#include <cmath>

using namespace std;


int main() {
    float x, y, z;
    cin>>x>>y;
    if(x == 0){
        if (y >= -5 && y<=5){
            cout<<"Попал в первый сектор\n";
        }
        else if((y >=10 && y<=15) || (y >=-15 && y<=-10)){
            cout<<"Попал во второй сектор\n";
        }
        else if((y >=20 && y<=25) || (y >=-25 && y<=-20)){
            cout<<"Попал в третий сектор\n";
        }
        else{
            cout<<"Не попал\n";
        }
    }
    else if(y == 0){
        if (x >= -5 && x<=5){
            cout<<"Попал в первый сектор\n";
        }
        else if((x >=10 && x<=15) || (x >=-15 && x<=-10)){
            cout<<"Попал во второй сектор\n";
        }
        else if((x >=20 && x<=25) || (x >=-25 && x<=-20)){
            cout<<"Попал в третий сектор\n";
        }
        else{
            cout<<"Не попал\n";
        }
    }
    else{
        z = sqrt(x*x+y*y);
        if (z >=0 && z<=5){
            cout<<"Попал в первый сектор\n";
        }
        else if (z >=10 && z<=15){
            cout<<"Попал во второй сектор\n";
        }
        else if(z >=20 && z<=25){
            cout<<"Попал в третий сектор\n";
        }
        else{
            cout<<"Не попал";
        }
    }

    return 0;
}
