#include <iostream>
#include <cmath>

using namespace std;


int main() {
    float x, y;
    cin>>x>>y;

    if(y>=x && (y>=0 && y<=3) && (x>=0 && x<=3)){
        cout<<"Попал";
    }
    else{
        cout<<"Не попал";
    }

    return 0;
}
